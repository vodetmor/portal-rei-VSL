{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user in the system.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the User entity." },
        "email": { "type": "string", "description": "Email address of the user.", "format": "email" },
        "displayName": { "type": "string", "description": "Public display name of the user." },
        "photoURL": { "type": "string", "description": "URL of the user's profile picture.", "format": "uri" },
        "role": { "type": "string", "description": "Role of the user in the system.", "enum": ["admin", "user"] }
      },
      "required": ["id", "email"]
    },
    "Course": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "Course",
      "type": "object",
      "description": "Represents a course in the platform.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Course entity." },
        "title": { "type": "string", "description": "Title of the course." },
        "subtitle": { "type": "string", "description": "Subtitle of the course, displayed below the main title." },
        "description": { "type": "string", "description": "Description of the course." },
        "modules": {
          "type": "array",
          "description": "An array of modules belonging to the course.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "title": { "type": "string" },
              "thumbnailUrl": { "type": "string", "format": "uri" },
              "imageHint": { "type": "string" },
              "releaseDelayDays": { "type": "number", "description": "Number of days after enrollment to release this module." },
              "lessons": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "id": { "type": "string" },
                    "title": { "type": "string" },
                    "videoUrl": { "type": "string", "format": "uri" },
                    "description": { "type": "string" },
                    "releaseDelayDays": { "type": "number", "description": "Number of days after enrollment to release this lesson." }
                  },
                  "required": ["id", "title"]
                }
              }
            },
            "required": ["id", "title"]
          }
        }
      },
      "required": ["id", "title", "description"]
    },
    "CourseAccess": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "CourseAccess",
      "type": "object",
      "description": "Represents a user's access grant to a specific course.",
      "properties": {
        "courseId": { "type": "string", "description": "The ID of the course access is granted to." },
        "grantedAt": { "type": "string", "description": "Timestamp when access was granted.", "format": "date-time" }
      },
      "required": ["courseId", "grantedAt"]
    },
    "Video": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "Video",
      "type": "object",
      "description": "Represents a video in the portfolio.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Video entity." },
        "title": { "type": "string", "description": "Title of the video." },
        "description": { "type": "string", "description": "Description of the video." },
        "url": { "type": "string", "description": "URL of the video.", "format": "uri" },
        "aspectRatio": { "type": "string", "description": "Aspect ratio of the video (e.g., '16:9', '9:16')." }
      },
      "required": ["id", "title", "description", "url", "aspectRatio"]
    },
    "Client": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "Client",
      "type": "object",
      "description": "Represents a client whose logo is displayed in the carousel.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Client entity." },
        "name": { "type": "string", "description": "Name of the client." },
        "logoUrl": { "type": "string", "description": "URL of the client's logo image.", "format": "uri" }
      },
      "required": ["id", "name", "logoUrl"]
    },
    "Skill": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "Skill",
      "type": "object",
      "description": "Represents a skill showcased on the website.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Skill entity." },
        "name": { "type": "string", "description": "Name of the skill (e.g., 'YouTube Editing')." },
        "description": { "type": "string", "description": "Description of the skill." },
        "iconUrl": { "type": "string", "description": "URL of the icon representing the skill.", "format": "uri" }
      },
      "required": ["id", "name", "description", "iconUrl"]
    },
    "ContactMessage": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "ContactMessage",
      "type": "object",
      "description": "Represents a message submitted through the contact form.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the ContactMessage entity." },
        "name": { "type": "string", "description": "Name of the sender." },
        "email": { "type": "string", "description": "Email address of the sender.", "format": "email" },
        "message": { "type": "string", "description": "The message content." },
        "sentAt": { "type": "string", "description": "Timestamp indicating when the message was sent.", "format": "date-time" }
      },
      "required": ["id", "name", "email", "message", "sentAt"]
    },
    "PremiumLink": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "PremiumLink",
      "type": "object",
      "description": "Represents a unique link that grants access to one or more courses.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the link (the token)." },
        "name": { "type": "string", "description": "A descriptive name for the link, for admin use." },
        "courseIds": {
          "type": "array",
          "description": "An array of course IDs that this link grants access to.",
          "items": { "type": "string" }
        }
      },
      "required": ["id", "name", "courseIds"]
    },
    "AuditLog": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "AuditLog",
        "type": "object",
        "description": "Represents a log entry for an action performed by an administrator.",
        "properties": {
          "id": { "type": "string", "description": "Unique identifier for the log entry." },
          "adminId": { "type": "string", "description": "The UID of the administrator who performed the action." },
          "adminEmail": { "type": "string", "description": "The email of the administrator." },
          "action": { "type": "string", "description": "A description of the action performed (e.g., 'course_created', 'module_updated')." },
          "entityType": { "type": "string", "description": "The type of entity that was affected (e.g., 'course', 'user', 'module')." },
          "entityId": { "type": "string", "description": "The ID of the entity that was affected." },
          "entityTitle": { "type": "string", "description": "The title or name of the entity for easy identification." },
          "timestamp": { "type": "string", "format": "date-time", "description": "When the action occurred." }
        },
        "required": ["id", "adminId", "adminEmail", "action", "entityType", "entityId", "timestamp"]
    },
    "Progress": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "Progress",
        "type": "object",
        "description": "Tracks a user's progress in a specific course.",
        "properties": {
          "courseId": { "type": "string", "description": "The ID of the course this progress pertains to." },
          "completedLessons": {
            "type": "object",
            "description": "A map where keys are lesson IDs and values are timestamps of completion.",
            "additionalProperties": { "type": "string", "format": "date-time" }
          },
          "updatedAt": { "type": "string", "format": "date-time", "description": "The last time progress was updated." }
        },
        "required": ["courseId", "completedLessons", "updatedAt"]
      }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "Stores user profile information.",
          "params": [{ "name": "userId", "description": "The unique ID of the user." }]
        }
      },
       {
        "path": "/courses/{courseId}",
        "definition": {
          "entityName": "Course",
          "schema": { "$ref": "#/backend/entities/Course" },
          "description": "Stores course data.",
          "params": [{ "name": "courseId", "description": "Unique identifier for the course." }]
        }
      },
      {
        "path": "/users/{userId}/courseAccess/{courseId}",
        "definition": {
          "entityName": "CourseAccess",
          "schema": { "$ref": "#/backend/entities/CourseAccess" },
          "description": "Stores a record of a user's access to a specific course.",
          "params": [
            { "name": "userId", "description": "The ID of the user." },
            { "name": "courseId", "description": "The ID of the course." }
          ]
        }
      },
      {
        "path": "/users/{userId}/progress/{courseId}",
        "definition": {
            "entityName": "Progress",
            "schema": { "$ref": "#/backend/entities/Progress" },
            "description": "Tracks a user's lesson completion for a specific course.",
            "params": [
              { "name": "userId", "description": "The ID of the user." },
              { "name": "courseId", "description": "The ID of the course." }
            ]
        }
      },
      {
        "path": "/videos/{videoId}",
        "definition": {
          "entityName": "Video",
          "schema": { "$ref": "#/backend/entities/Video" },
          "description": "Stores video data for the portfolio.",
          "params": [{ "name": "videoId", "description": "Unique identifier for the video." }]
        }
      },
      {
        "path": "/clients/{clientId}",
        "definition": {
          "entityName": "Client",
          "schema": { "$ref": "#/backend/entities/Client" },
          "description": "Stores client logos for the carousel.",
          "params": [{ "name": "clientId", "description": "Unique identifier for the client." }]
        }
      },
      {
        "path": "/skills/{skillId}",
        "definition": {
          "entityName": "Skill",
          "schema": { "$ref": "#/backend/entities/Skill" },
          "description": "Stores skills to be displayed on the website.",
          "params": [{ "name": "skillId", "description": "Unique identifier for the skill." }]
        }
      },
      {
        "path": "/contactMessages/{messageId}",
        "definition": {
          "entityName": "ContactMessage",
          "schema": { "$ref": "#/backend/entities/ContactMessage" },
          "description": "Stores contact messages submitted through the contact form.",
          "params": [{ "name": "messageId", "description": "Unique identifier for the contact message." }]
        }
      },
       {
        "path": "/premiumLinks/{linkId}",
        "definition": {
          "entityName": "PremiumLink",
          "schema": { "$ref": "#/backend/entities/PremiumLink" },
          "description": "Stores premium access links and their associated course IDs.",
          "params": [{ "name": "linkId", "description": "The unique token for the premium link." }]
        }
      },
      {
        "path": "/auditLogs/{logId}",
        "definition": {
          "entityName": "AuditLog",
          "schema": { "$ref": "#/backend/entities/AuditLog" },
          "description": "Stores a log of all actions performed by administrators for auditing purposes.",
          "params": [{ "name": "logId", "description": "The unique ID for the log entry." }]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to store data for the 'Portfólio Hades' application, focusing on separation of concerns, security, and scalability. User management has been added with a specific subcollection for course access to allow granular control. The `premiumLinks` collection has been added to manage the new premium access system, linking tokens to course bundles.\n\n**Users:** Stored in the `/users` collection.\n**Course Access:** A subcollection `/users/{userId}/courseAccess` explicitly grants users access to courses.\n**Premium Links:** A new collection `/premiumLinks` stores the generated tokens and the course IDs they grant access to.\n**Audit Logs:** A new collection `/auditLogs` has been added to track all administrative actions for security and monitoring purposes.\n**Progress:** A new subcollection `/users/{userId}/progress` tracks user completion of lessons within each course, enabling progress bars and detailed admin views."
  }
}
